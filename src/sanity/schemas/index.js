import profile from './profile';
import skill from './skill';
import project from './project';
import contactMessage from './message';

export const schemaTypes = [profile, skill, project, contactMessage];
